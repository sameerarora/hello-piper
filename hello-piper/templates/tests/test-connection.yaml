apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-piper.fullname" . }}-test-connection"
  labels:
    {{- include "hello-piper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-piper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
