# Default values for helm-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

applicationName: hello-piper

image: sameerarora11/hello-piper

service:
  type: LoadBalancer